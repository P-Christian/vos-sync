import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['vos-sync-local', 'vertex-dev', '100.114.249.96', '100.119.3.44']
};

export default nextConfig;
